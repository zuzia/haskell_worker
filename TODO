* pipeline data flow
* grouping: group_node, group_label
* exception handling, that implies implementation of ERROR, FATAL, INPUT_ERR messages from the worker protocol
* dir scheme handling, test
* input polling ("more" flag, input exclude, include)
* System.IO.Streams (?) in Reader
* reduce stage - sorting inputs
* util module
* multiple replicas's locations

